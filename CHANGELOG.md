
## 2026-06-28 Ctrl+C 交互修复
- 修复新增/编辑/删除账号过程中按 Ctrl+C 会显示 Python traceback 的问题。
- 现在中途取消会显示“已取消当前操作，返回菜单”，不会保存半截账号。
- 保留 Telegram 全局配置菜单 t，账号业务仍在单账号菜单中操作。

# 更新记录

## 当前版

- 保留首页 `t. TG Bot / 日志` 菜单。
- 增加查看 Telegram 配置，敏感 Token 自动打码。
- 增加修改 Telegram Bot Token、频道/群组 ID、管理员私聊 ID。
- 增加发送测试消息到频道/群组、发送测试消息到管理员私聊。
- 明确新增阿里云账号不重复填写 Telegram 配置；Telegram 配置为全局配置。


## 2026-06-28 首页错误输出修复

- 修复 `cdt` 首页查询账号状态时，AccessKey/实例 ID 错误会把阿里云 SDK 完整报错打印到菜单的问题。
- 首页现在只显示账号列表和简短状态，例如 `状态未知：Unknown`。
- 账号详细错误仍可通过日志菜单查看。



## 2026-06-28

- 修复 cdt 首页账号列表报错：`account_keepalive_enabled` 未定义。
- 首页账号运行状态改为纯中文文字，避免部分 SSH 终端 emoji 显示异常。
# CHANGELOG

## 多账号版

- 增加 `accounts.json` 多账号配置。
- 第一次安装只录入 1 个账号。
- `cdt` 菜单新增“多账号管理”：查看、增加、删除、编辑、启用、禁用账号。
- cron 每分钟遍历所有启用账号。
- 每个账号独立阈值、日报时间、流量熔断通知、月初自动开机通知。
- Telegram `/status`、`/traffic`、`/bill`、`/report` 支持所有账号汇总。
- Telegram `/startvps 账号名或序号`、`/stopvps 账号名或序号` 支持指定账号。
- 继续保留：北京时间日报、流量熔断通知只发一次、月初 00:05 后检测自动开机。

## 菜单结构优化

- `cdt` 进入后先显示所有账号列表。
- 输入账号序号后进入单账号控制菜单，可单独查看状态、发送日报、开机、关机、执行检查、编辑、启用、禁用、删除。
- 主菜单保留增加账号、删除账号、全局设置、完全卸载程序。
- 全局设置中集中管理 TG Bot 服务、全局保活、日志、crontab、通知状态和配置文件。


## 交互修复
- 修复新增/编辑账号时无默认值字段的输入提示缺少冒号，导致输入内容和提示文字粘在一起的问题。
- 打包时排除 __pycache__ 缓存目录。

## 2026-06-28 菜单职责调整

- `cdt` 主菜单改为先列出账号，输入账号序号后进入该账号单独控制。
- 开机、关机、立即检查、立即日报、自动保活开关全部移动到单账号菜单。
- `g. 服务 / 日志 / 工具` 只保留 TG Bot 服务、日志、crontab、系统时间、配置文件查看/编辑等工具项。
- 新增每个账号独立 `auto_keepalive` 自动保活开关。

## 2026-06-28

- cdt 首页账号列表增加账号当前状态显示：机器在线运行中、流量达到阈值已关机、手动关机、正在开机/关机、外部未知关机等。
- 手动关机现在会记录 manual_stop 状态；手动开机会清除 manual_stop 和 traffic_stop 状态。
- 流量熔断关机会优先标记为 traffic_stop，避免和手动关机混淆。

## 2026-06-28 菜单逻辑调整
- `g` 改为“选择账号操作”，进入后先选账号，再进入该账号单独控制。
- 服务、日志、工具菜单移动到 `s`。
- 单账号菜单顶部显示当前账号列表行，包含账号名、状态、区域、实例、阈值等信息。

## 菜单简化
- 移除 `s. 服务 / 日志 / 工具` 入口。
- 首页只保留账号序号、增加账号、删除账号、卸载程序、退出。
- 输入账号序号后直接进入该账号的单独控制菜单。

## 2026-06-28 TG 菜单恢复
- 首页保留极简账号操作，同时恢复 `t. TG Bot / 日志` 入口。
- 账号开机、关机、日报、检查、保活仍然只在单账号菜单里操作。
- TG Bot 服务状态、启动、停止、重启、日志、crontab、通知状态、配置查看编辑放入 `t` 菜单。
